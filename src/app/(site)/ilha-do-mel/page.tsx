import { brl } from "@/lib/format";
import postgres from "postgres";
export const dynamic = "force-dynamic";
export default async function IlhaDoMelPage() {
  const sql=postgres(process.env.DATABASE_URL!,{max:1}); const [p]=await sql`SELECT * FROM pousada LIMIT 1`; const passeios=await sql`SELECT * FROM passeios WHERE ativo=true ORDER BY ordem`; await sql.end();
  return(<div className="max-w-7xl mx-auto px-4 py-12"><h1 className="text-3xl font-bold mb-2">Ilha do Mel</h1><p className="text-gray-500 mb-8">Tudo que você precisa saber para chegar e aproveitar</p><div className="grid grid-cols-1 md:grid-cols-2 gap-8 mb-12"><div><h2 className="text-xl font-semibold mb-4">Como Chegar</h2><div className="prose text-sm text-gray-600" dangerouslySetInnerHTML={{__html:p?.como_chegar?.replace(/\n/g,"<br/>")||"Acesso por barco saindo de Pontal do Sul (30 min, R$ 30) ou Paranaguá (1h30, R$ 25)."}}/></div><div><h2 className="text-xl font-semibold mb-4">Localização</h2><div className="bg-gray-200 rounded-xl h-64 flex items-center justify-center"><span>📍 {p?.lat}, {p?.lng}</span></div></div></div><h2 className="text-2xl font-bold mb-6">Passeios e Experiências</h2><div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">{passeios.map((p:any)=>(<div key={p.id} className="bg-white rounded-xl shadow p-6"><h3 className="font-semibold">{p.nome}</h3><p className="text-sm text-gray-500 mt-1">{p.descricao}</p><div className="flex justify-between mt-3 text-xs text-gray-400"><span>{p.duracao}</span>{p.preco_referencia&&<span>a partir de {brl(p.preco_referencia)}</span>}</div></div>))}</div></div>);
}