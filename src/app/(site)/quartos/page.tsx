import postgres from "postgres"; import Link from "next/link";
export const dynamic = "force-dynamic";
export default async function QuartosPage() {
  const sql=postgres(process.env.DATABASE_URL!,{max:1}); const lista=await sql`SELECT q.*, c.nome as cat_nome FROM quartos q LEFT JOIN categorias c ON q.categoria_id=c.id WHERE q.ativo=true ORDER BY q.ordem`; await sql.end();
  return(<div className="max-w-7xl mx-auto px-4 py-12"><h1 className="text-3xl font-bold mb-2">Quartos</h1><p className="text-gray-500 mb-8">Conheca nossas opcoes na Ilha do Mel</p><div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">{lista.map((q:any)=>(<Link key={q.id} href={`/quartos/${q.slug}`} className="bg-white rounded-xl shadow hover:shadow-lg overflow-hidden group"><div className="h-48 bg-gradient-to-br from-teal-100 to-teal-200 flex items-center justify-center"><span className="text-5xl">🏨</span></div><div className="p-4">{q.cat_nome && <span className="text-xs text-teal-600 bg-teal-50 px-2 py-0.5 rounded">{q.cat_nome}</span>}<h3 className="font-semibold text-lg mt-1 group-hover:text-teal-600">{q.nome}</h3><p className="text-sm text-gray-500 mt-1">{q.descricao?.slice(0,100)}</p><div className="text-sm text-gray-600 mt-2">Ate {q.ocupacao_max} pessoas {q.metragem && `• ${q.metragem}m²`}</div></div></Link>))}</div></div>);
}